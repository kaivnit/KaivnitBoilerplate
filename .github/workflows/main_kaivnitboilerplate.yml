# GitHub Actions workflow: Build & Deploy ASP.NET Core self-contained to Azure WebApp

name: Build and deploy ASP.Net Core app to Azure Web App - KaivnitBoilerplate

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read # required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Restore
        run: dotnet restore src/KaivnitBoilerplate.Presentation/KaivnitBoilerplate.Presentation.csproj

      - name: Publish self-contained (win-x64)
        run: >
          dotnet publish src/KaivnitBoilerplate.Presentation/KaivnitBoilerplate.Presentation.csproj
          -c Release
          -r win-x64
          --self-contained true
          -p:PublishSingleFile=true
          -p:PublishTrimmed=false
          -p:UseAppHost=true
          -o "${{ github.workspace }}/publish"

      - name: Archive package (no nested folder)
        run: |
          powershell -command "Compress-Archive -Path 'publish\*' -DestinationPath 'package.zip' -Force"

      - name: Upload artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: sc-app
          path: package.zip

  deploy:
    runs-on: windows-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build
        uses: actions/download-artifact@v4
        with:
          name: sc-app
          path: .

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_3729D6BBC2144313B592887D4D0522F9 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_E7B0B0932A4E40BF9B51C316F8B8AFAA }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_4640C759979F496DA8D8EB581E4F0C82 }}

      - name: Deploy to Azure Web App (Zip Deploy)
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'KaivnitBoilerplate'
          slot-name: 'Production'
          package: package.zip
